pr_data_diff:
  target: default
  outputs:
    default:
      type: bigquery
      method: oauth
      project: "{{ env_var('GCP_PROJECT_ID') }}"
      dataset: "{{ env_var('BQ_DATASET') }}"
      location: US
      threads: 4
